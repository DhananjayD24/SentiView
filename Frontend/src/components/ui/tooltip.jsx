// src/components/ui/tooltip.jsx
export function TooltipProvider({ children }) {
  // For now, just render children directly.
  // You can later replace this with a full Radix Tooltip implementation.
  return children;
}
